#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../dgeni/bin/gen-docs.js" "$@"
  ret=$?
else 
  node  "$basedir/../dgeni/bin/gen-docs.js" "$@"
  ret=$?
fi
exit $ret
